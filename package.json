{
  "name": "csv2json",
  "version": "1.0.0",
  "description": "Project to convert csv to json object",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies":{
    "fast-csv" : ">= 0.0.x",
    "deepmerge" : ">= 0.0.x",
    "dateformat" : ">=0.0.x"
  },

  "author": "Jonathan Fidelis Paul",
  "license": "ISC"
}
